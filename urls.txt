(anteponer api/manage/)

workspaces/{id}
   
user/{id_user}
user/login                  POST

(anteponer api/event/)

events/                  POST
events/{id}
events/{id}/details  
future-activity/ (para obtener todos los agendamientos y ver disponibilidad de todos los dias)
future-activity/q?all={true o false}&spaces={id_space} (para obtener los agendamientos de uno o varios espacios en específico)

(anteponer api/)

memos/memories/
memos/{id}/
memos/download/{id}/
